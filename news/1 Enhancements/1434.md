Changes to how debug options are configured in the new experimental debugger. 
Settings configured within the `debugOptions` property of the old debugger are now defined as individual (boolean) properties.
